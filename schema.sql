CREATE DATABASE languagedev-study_db1,
CREATE TABLE [IF NOT EXISTS] Login (
    Name varchar(255) NOT NULL,
    Username varchar(255) NOT NULL,
    Password varchar(255) NOT NULL,
    Email varchar(255),
),